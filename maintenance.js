/* =========================================================
   PLEXO GRAPHICS
   FULL SCREEN STATIC MAINTENANCE PAGE
   ========================================================= */

document.addEventListener("DOMContentLoaded", () => {

    /*
        No animations or heavy JavaScript are used.

        This file is intentionally kept simple so the
        maintenance page loads quickly and reliably.
    */

    console.log("PLEXO GRAPHICS — Maintenance Mode");

});